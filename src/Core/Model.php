<?php

namespace Core;

/**
 * Base model
 * @version: PHP: 8.1
 *
 * @Model
 */
abstract class Model extends Database {
    /**
     * Nothing in here yet, let us see what will become of this.
     * Although it extends the Database (Abstract) object giving us access to the
     * database object. So it does something on behalf of something else.
     */
}
